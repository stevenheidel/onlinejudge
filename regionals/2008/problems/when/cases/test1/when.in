when ( $started$ < 1 )
  set $i$ = 1 , $j$ = 1 , $run$ = 1 , $started$ = 1
end when
when ( $run$ and ( $i$ < 11 ) )
  print $i$ , $j$
  set $i$ = ( $i$ + 1 )
end when
when ( $run$ and ( 10 < $i$ ) )
  set $i$ = 1 , $j$ = ( $j$ + 1 )
end when
when ( $run$ and ( 10 < $j$ ) )
  set $run$ = 0
end when
